// Decimal serialization helper intentionally left as a no-op.
// We convert Decimal values to numbers in service/controller DTO mapping where needed.
export function patchDecimalSerialization(): void {
  // no-op
}
